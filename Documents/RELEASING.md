# Releasing

## Versioning
- Tag format: `vX.Y.Z` (e.g., `v0.1.0-beta`)
- Keep `CHANGELOG.md` updated.

## Create a release
1. `git tag vX.Y.Z`
2. `git push --tags`
3. GitHub → Releases → Draft a new release
4. Attach any distribution ZIP you want to provide (optional).
